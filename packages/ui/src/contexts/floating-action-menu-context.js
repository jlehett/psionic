import { createContext } from 'react';

// Floating Action Menu Open Context
export const FloatingActionMenuOpen = createContext(false);

// Set Floating Action Menu Open Context
export const SetFloatingActionMenuOpen = createContext(() => {});