import { createContext } from 'react';

// Form Data Context
export const FormData = createContext({});

// Set Form Data Context
export const SetFormData = createContext(() => {});

// Form Submitting Context
export const FormSubmitting = createContext(() => {});

// Set Form Submitting Context
export const SetFormSubmitting = createContext(() => {});
